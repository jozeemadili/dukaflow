info:
  name: Get supplier
  type: http
  seq: 3
http:
  method: GET
  url: '{{base_url}}/suppliers/{{supplier_id}}'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
