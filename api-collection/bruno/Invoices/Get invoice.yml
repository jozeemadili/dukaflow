info:
  name: Get invoice
  type: http
  seq: 3
http:
  method: GET
  url: '{{base_url}}/invoices/{{invoice_id}}'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
