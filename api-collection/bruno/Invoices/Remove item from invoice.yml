info:
  name: Remove item from invoice
  type: http
  seq: 6
http:
  method: DELETE
  url: '{{base_url}}/invoices/{{invoice_id}}/items/{{item_id}}'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
