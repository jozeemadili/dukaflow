info:
  name: Approve invoice (proforma -> invoice)
  type: http
  seq: 8
http:
  method: POST
  url: '{{base_url}}/invoices/{{invoice_id}}/approve'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
