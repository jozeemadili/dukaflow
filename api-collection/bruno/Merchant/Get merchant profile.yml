info:
  name: Get merchant profile
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/merchant'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
