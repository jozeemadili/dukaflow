info:
  name: Get stock receipt
  type: http
  seq: 3
http:
  method: GET
  url: '{{base_url}}/stock-receipts/{{receipt_id}}'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
