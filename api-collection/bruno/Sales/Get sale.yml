info:
  name: Get sale
  type: http
  seq: 2
http:
  method: GET
  url: '{{base_url}}/sales/{{sale_id}}'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
