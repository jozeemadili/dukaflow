info:
  name: Current user (me)
  type: http
  seq: 5
http:
  method: GET
  url: '{{base_url}}/auth/me'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
