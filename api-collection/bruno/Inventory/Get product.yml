info:
  name: Get product
  type: http
  seq: 5
http:
  method: GET
  url: '{{base_url}}/inventory/{{item_id}}'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
