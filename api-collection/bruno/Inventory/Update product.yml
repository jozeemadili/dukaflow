info:
  name: Update product
  type: http
  seq: 6
http:
  method: PATCH
  url: '{{base_url}}/inventory/{{item_id}}'
  body:
    type: json
    data: |-
      {
        "name": "Rice 2kg",
        "unit_price": 5800
      }
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
