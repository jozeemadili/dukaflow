info:
  name: List categories
  type: http
  seq: 3
http:
  method: GET
  url: '{{base_url}}/inventory/categories'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
