info:
  name: Create branch / store
  type: http
  seq: 2
http:
  method: POST
  url: '{{base_url}}/branches'
  body:
    type: json
    data: |-
      {
        "name": "Main Branch",
        "address": "",
        "phone": ""
      }
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
