info:
  name: List branches / stores
  type: http
  seq: 1
http:
  method: GET
  url: '{{base_url}}/branches'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
