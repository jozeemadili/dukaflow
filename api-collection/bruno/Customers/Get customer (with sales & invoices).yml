info:
  name: Get customer (with sales & invoices)
  type: http
  seq: 3
http:
  method: GET
  url: '{{base_url}}/customers/{{customer_id}}'
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
